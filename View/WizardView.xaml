<mah:MetroWindow x:Class="ClipboardWizard.View.WizardView"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                 xmlns:vm="clr-namespace:ClipboardWizard.ViewModel"
                 xmlns:fa5="http://schemas.fontawesome.com/icons/" 
                 xmlns:uc="clr-namespace:ClipboardWizard.View.Control"
                 mc:Ignorable="d"
                 Title="Clipboard Wizard"
                 Height="450"
                 Width="800"
                 MinWidth="200">

    <Window.Resources>
        <vm:WizardViewModel x:Key="vm" />
    </Window.Resources>

    <mah:MetroWindow.IconTemplate>
        <DataTemplate>
            <Grid Width="{TemplateBinding Width}"
                 Height="{TemplateBinding Height}"
                 Margin="4"
                 Background="Transparent"
                 RenderOptions.EdgeMode="Aliased"
                 RenderOptions.BitmapScalingMode="HighQuality">
                <Image Source="../witch-hat-white.png"></Image>
            </Grid>
        </DataTemplate>
    </mah:MetroWindow.IconTemplate>
    
    <mah:MetroWindow.RightWindowCommands>
        <mah:WindowCommands DataContext="{StaticResource vm}">
            <mah:ToggleSwitch  OnContent="Recording ON" OffContent="Recording OFF" IsOn="{Binding Recording, Mode=TwoWay}" Foreground="White" />
        </mah:WindowCommands>
    </mah:MetroWindow.RightWindowCommands>

    <DockPanel DataContext="{StaticResource vm}">
        <DockPanel DockPanel.Dock="Bottom" Margin="5">
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Command="{Binding SaveClipboardContents}" Focusable="False">
                    <fa5:FontAwesome Icon="Solid_Plus" />
                </Button>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Button Command="{Binding AddSnippet}" Focusable="False" Content="New"></Button>
            </StackPanel>
        </DockPanel>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" AllowDrop="True" Drop="ScrollViewer_Drop" >
            <ItemsControl ItemsSource="{Binding SnippetViewModels}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid x:Name="SnippetsUniformGrid"
                                     VerticalAlignment="Top"
                                     Loaded="SnippetsUniformGrid_Loaded"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <uc:SnippetControl PreviewMouseMove="SnippetControl_PreviewMouseMove" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>

</mah:MetroWindow>
