<mah:MetroWindow x:Class="ClipboardWizard.View.AddCategoryView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        mc:Ignorable="d"
        Title="New Category"
        MaxWidth="1600"
        MinWidth="300"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        d:DesignWidth="400" ShowInTaskbar="False" WindowStyle="ToolWindow" Activated="Window_Activated">

    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 10 0 0">
            <Button Content="Save" IsEnabled="{Binding IsValid}" Click="Save_Click" Margin="5" TabIndex="2" IsDefault="True" />
            <Button Content="Cancel" Click="Cancel_Click" Margin="5" TabIndex="3" IsCancel="True" />
        </StackPanel>
        <StackPanel DockPanel.Dock="Top">
            <Label Content="Name" />
            <TextBox x:Name="NameTextBox" Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TabIndex="1" />
        </StackPanel>
    </DockPanel>
</mah:MetroWindow>
